# Sample notes

This markdown file exists so tests can assert that non-code files do **not**
get complexity metrics. Even though prose contains words like if, for, while,
and switch, there is no control flow to measure here.

- item one
- item two

See `math.rs` for the real code.
