[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# The generated CI is security-hardened by hand; cargo-dist must not overwrite it
allow-dirty = ["ci"]
# CI backends to support
ci = "github"
# Embed dependency metadata in binaries and publish a CycloneDX SBOM
cargo-auditable = true
cargo-cyclonedx = true
# The installers to generate for each app
installers = ["shell"]
# Additional legal notices shipped beside the binary
include = ["CONTRIBUTING.md", "SECURITY.md", "THIRD_PARTY_NOTICES.md", "docs/"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false
